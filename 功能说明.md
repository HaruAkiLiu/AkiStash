# AkiStash

一个运行在浏览器里的二维 / 三维绘图软件 Demo。整个程序只有一个 HTML 文件，不需要安装、不需要编译，双击即可打开使用。

二维部分支持基本图形、自由书写（含笔压与手颤修正）、透视尺与对称尺、橡皮擦、油漆桶、选区工具、图片导入、多图层与混合模式；三维部分支持基本几何体、一个带肌肉起伏且可以自由摆姿势的人体模型，以及机械制图中的三面投影演示。作品可以保存为 JSON 文件并重新打开，也可以导出为 PNG 图片。

软件标志（抱着调色板的仓鼠）由作者绘制，内嵌在页面中：浅色模式下显示为黑色，深色模式下自动变为白色；浏览器标签页图标也会随系统主题切换。

---

## 快速开始

1. 下载 `draw-studio.html`（网页版）。
2. 用浏览器直接打开（双击文件即可）。
3. 从左侧工具栏选择工具，在画布上开始绘制。

> **需要联网**：三维场景依赖 Three.js，界面字体来自 Google Fonts，都通过 CDN 加载。断网时二维绘图仍可正常使用，界面会退回系统字体，三维场景会提示加载失败。

### 浏览器要求

推荐使用最新版的 Chrome、Edge、Firefox 或 Safari。

| 功能 | 要求 |
| --- | --- |
| 二维绘图、图层、混合模式 | 所有现代浏览器 |
| 图层调整参数（亮度、对比度、模糊等） | 需要浏览器支持画布滤镜（Canvas `filter`），不支持时会自动禁用并提示 |
| 三维场景 | 需要支持 WebGL |
| 笔压感应 | 需要手写笔或数位板；鼠标和触屏用书写速度模拟 |

---

## 功能一览

### 二维绘图

**绘图工具**

| 工具 | 快捷键 | 说明 |
| --- | --- | --- |
| 画笔 | `B` | 按住拖动自由书写，曲线自动平滑，支持笔压 |
| 点 | `P` | 单击画点 |
| 直线 | `L` | 拖动绘制，按住 `Shift` 锁定 45° |
| 矩形 | `R` | 拖动绘制，按住 `Shift` 画正方形 |
| 圆 | `C` | 从圆心向外拖动 |
| 椭圆 | `E` | 拖动绘制，按住 `Shift` 画正圆 |
| 多边形 | `G` | 逐点单击；点击起点、双击或按 `Enter` 完成，`Esc` 取消 |
| 橡皮擦 | `X` | 局部擦除，或擦除整个图形 |
| 油漆桶 | `F` | 区域填充，或图形填充 |

**图形属性**

- 线条：颜色、线宽（1–30）、线型（实线 / 虚线 / 点线）、透明度。
- 填充：无、纯色、斜线、网格、圆点，以及填充颜色。
- 位置与尺寸：选中后可以直接输入 X、Y、宽、高。

**画笔与笔压**

- 使用手写笔或数位板时读取真实压力，按得越重线条越粗。
- 使用鼠标或手指时，用书写速度模拟笔压：写得慢线条粗，写得快线条细。
- 笔画会自动平滑，并用 Ramer–Douglas–Peucker 算法精简采样点，减小文件体积。
- 半透明笔画的颜色是均匀的，分段重叠处不会变深。
- **手颤修正**（0–100）：数值越大线条越平滑稳定，笔迹会稍微落后于光标，松手时自动补齐；设为 0 即关闭。

**笔刷与画笔工作室**

选中画笔工具后，右侧才会出现“笔刷”设置，平时不占界面空间。

- 预设笔刷：标准圆笔、墨水笔、铅笔、马克笔、书法笔、喷枪、粉笔、水彩、点画、彩色纸屑。
- 点击“打开画笔工作室”可以细调参数，右侧的绘图板可以随手试画，参数改动实时生效：
  - 描边路径：间距、间距抖动、横向抖动、线性抖动、掉落
  - 稳定性：手颤修正
  - 锥度：起笔锥度、收笔锥度、最细处尺寸
  - 形状：圆形或方形笔尖、圆度、角度、笔尖跟随笔画方向、边缘硬度
  - 颗粒：颗粒强度、颗粒大小（纹理固定在画布上）
  - 渲染：流量
  - 颜色动态：色相、饱和度、亮度、透明度抖动
  - 笔压：笔压感应开关、鼠标速度模拟、压力影响大小、压力影响透明度
  - 属性：大小抖动
  - 关于此画笔：名称，导出 / 导入笔刷（`.brush.json`），恢复预设
- 自定义笔刷会保存在浏览器里，下次打开自动沿用。每一笔都会记录当时的笔刷参数和随机种子，保存作品后重新打开，笔迹完全一致。
- 实现方式：与标准圆笔一致时仍用矢量曲线绘制；其他笔刷沿平滑后的路径按间距放置“笔触”，由固定种子的随机数决定抖动，整笔先画到临时画布再叠加颗粒纹理和透明度。

**辅助尺**

选择画笔、橡皮擦或任意图形工具时，右侧面板会出现“辅助尺”，同一时间可以启用一种：

- **透视尺**：支持一点透视和两点透视，拖动画布上的圆点可以移动消失点。开启“吸附”后，画笔落笔先朝大致方向划一小段，笔画就会锁定到最接近的透视线、水平线或竖直线；直线工具会实时吸附。
- **对称尺**：支持左右、上下、四向和径向对称。径向对称可设置 2–16 个分段，还可以勾选“每段再做镜像”，得到万花筒效果。拖动画布上的圆点移动对称中心。画笔、橡皮擦、点、直线、矩形、圆、椭圆、多边形都会同步生成对称副本。

**橡皮擦**

- **局部擦除**：像真实的橡皮一样擦掉经过的部分。
- **擦除整个图形**：碰到哪个图形就删除它。
- 两种模式都只作用于当前图层，大小可调，可以撤销。

**油漆桶**

- **区域填充**：按画面颜色找出与点击处相连的区域并填满，徒手画的封闭线条也能填。
  - 容差：数值越大，颜色相近的部分越容易被一起填进去。
  - 扩展：把填充向外撑 0–4 像素，盖住线条边缘，避免白边。
  - 参考：按所有可见图层判断边界，或只看当前图层。
  - 放在线条下方：填充插到当前图层线条的下面，不遮挡轮廓。
- **图形填充**：点击矩形、圆或多边形，直接设置它的矢量填充。
- 填充颜色与图案沿用“填充方式”中的设置。

**选择图形（选择工具 `V`）**

- 点击选中图形并拖动；空白处拖动为框选整个图形；`Shift` 点击加选或取消。
- 选中后可以：
  - 拖动移动。
  - 拖动 8 个控制点缩放，按住 `Shift` 保持比例。
  - 批量修改样式。
  - 复制或删除。
  - 用方向键微移。
  - 在图层内调整上下顺序，或移到其他图层。
- 隐藏或锁定图层中的图形不会被选中。

**选区（像素级）**

| 工具 | 快捷键 | 说明 |
| --- | --- | --- |
| 矩形选框 | `M` | 拖动画出矩形选区 |
| 椭圆选框 | `O` | 拖动画出椭圆选区 |
| 套索 | `Q` | 自由圈出选区，松开后自动闭合 |

- **选区会一直保留**，画面上以流动的虚线（蚂蚁线）显示，直到按 `Esc` 或点击“取消选区”。
- **加选与减选**：开始拖动时按住 `Shift` 为加选，按住 `Alt` 为减选；拖动途中按住 `Shift` 可画正方形或正圆。
- **绘画限制在选区内**：有选区时，画笔、各种图形、橡皮擦和油漆桶都只作用在选区内部，选区外的内容不会被改动。
- **只作用于当前图层的这块区域**，而不是整条笔迹：
  - 在选区内拖动：把当前图层中被选中的那部分内容剪出来并移动。
  - 按住 `Ctrl`（Mac 为 `Cmd`）在选区内拖动：只移动选区边框。
  - 清除内容（`Delete`）、拷贝（`Ctrl` + `C`）、剪切（`Ctrl` + `X`）、粘贴（`Ctrl` + `V`）、拷贝到新图层（`Ctrl` + `J`）。
- **其他操作**：反选（`Ctrl` + `Shift` + `I`）；填充选区（使用当前的填充颜色和图案）；选区工具下按 `Ctrl` + `A` 全选画布。

**画布操作**

- 滚轮缩放（10%–2000%）；“适应”按钮让所有图形显示在画布中。
- 按住空格或鼠标中键拖动平移，也可以使用平移工具（`H`）。
- 网格可以显示或隐藏。
- 撤销 / 重做（最多 200 步）。

### 导入图片

- 支持三种方式：点击顶部的“导入图片”，把图片文件直接拖进画布，或者按 `Ctrl` + `V` 粘贴剪贴板里的图片。
- 支持 PNG、JPG、GIF、WebP、SVG 等浏览器能识别的格式，可以一次导入多张。
- 导入的图片放在当前图层，会自动缩放到适合屏幕的大小，并处于选中状态。之后可以拖动、缩放、调整透明度、换图层，也可以用局部橡皮擦擦除。
- 导入线稿后，可以在上面新建图层，用油漆桶按线稿轮廓上色（参考选“所有可见图层”）。
- 边长超过 2048 像素的图片会自动缩小，以控制作品文件的大小。

### 图层

- 新建、删除、上移、下移、向下合并；双击图层名重命名。
- 每个图层可以单独隐藏或锁定。
- 图层透明度：同层内容先合成再整体变透明，重叠处不会越叠越深。
- 混合模式：

| 分组 | 模式 |
| --- | --- |
| 基本 | 正常 |
| 变暗 | 正片叠底、线性加深、颜色加深、变暗 |
| 变亮 | 滤色、颜色减淡、变亮 |
| 对比 | 叠加（覆盖）、柔光、强光 |
| 差值 | 差值、排除 |
| 颜色 | 色相、饱和度、颜色、明度 |

- 调整参数：亮度、对比度、饱和度、色相。
- 模糊（每个图层选一种）：
  - **高斯模糊**：半径 0–50，均匀柔化整个图层，适合景深、柔光和阴影。
  - **动态模糊**：距离 0–200、角度 0°–180°，沿指定方向拖出残影，模拟快速运动。
  - **透视模糊**：强度 0–100，以画布上可拖动的橙色准星为中心向外放射模糊，离中心越远越模糊，适合冲刺感和纵深效果。
- 处理顺序：先模糊，再调整颜色，最后按混合模式和图层透明度与下方内容混合。所有参数可以撤销，并随作品保存。

### 三维场景

- 添加几何体：立方体、球体、圆柱、圆锥、四棱锥、圆环。
- 视角：拖动空白处旋转，右键拖动平移，滚轮缩放，一键重置。
- 物体操作：
  - 拖动物体在地面上移动。
  - 按住 `Shift` 拖动为上下移动，按住 `Alt` 拖动为旋转。
  - 在右侧面板精确设置位置、旋转（角度）、缩放，并支持等比缩放。
- 外观：颜色、线框显示。
- 其他：复制、删除，显示或隐藏地面网格与坐标轴。

**三面投影**

点击三维工具栏的“三面投影”开启。场景中会出现互相垂直的三个投影面：正平面 V（后墙）、水平面 H（地面）、侧平面 W（右墙），以及投影轴 OX、OY、OZ。

- **坐标约定**与教材一致：x 从 W 面向左量，y 从 V 面向前量，z 从 H 面向上量，范围 0–6。
- **直线投影**：空间直线 AB 垂直投射到三个面上，得到水平投影 ab、正面投影 a′b′、侧面投影 a″b″；投影连线用细虚线画出。
- **编辑直线**：用滑块调整两个端点的 x、y、z；可以添加多条直线（依次命名为 AB、CD、EF……），各用不同颜色。
- **预设**：水平线、正平线、侧平线、铅垂线、正垂线、侧垂线、一般位置直线。
- **自动分析**：显示直线类型、实长、三个投影的长度（标出哪个反映实长、哪个积聚为点）、对三个投影面的倾角 α、β、γ，以及对应的投影特性说明。
- **投影展开图**：右下角实时绘制三面投影展开后的二维图，含 X、Z、Y<sub>H</sub>、Y<sub>W</sub> 轴和投影连线（用圆弧转换 Y 坐标），与教材中的画法一致。

**人体模型**

- 模型是一整张连续的蒙皮网格（`SkinnedMesh`），程序化生成：沿骨骼放样出带肌肉起伏的截面轮廓（三角肌、肱二头肌、大腿、腓肠肌等），关节处用球体过渡，顶点按距离在相邻骨骼之间加权混合，所以弯曲时表面是平滑连续的，不会出现断块或缝隙。
- 肌肉分区浮雕：在基础曲面上叠加一层局部起伏，表现胸大肌、锁骨、腹直肌（六块腹肌）、腹白线、腹外斜肌、背阔肌、斜方肌、肩胛骨、竖脊肌沟、臀大肌，以及三角肌、肱二头肌、肱三头肌、前臂屈肌群、股四头肌、股内侧肌、腘绳肌、腓肠肌、胫骨嵴、髌骨等。儿童和女性模型的肌肉起伏会自动减弱。
- 体型可在右侧“体型”里切换：成年男性、成年女性、男孩、女孩，按各自比例重新生成（儿童头身比更大、四肢更短；女性肩窄、髋宽）。鼻尖标示面朝方向。
- 可活动的关节：
  - 躯干：骨盆（整体朝向与高度）、腰部、胸部、脖子、头部。
  - 手臂：肩、肘、手腕，左右各一。
  - 腿：髋、膝、脚踝，左右各一。
  - 手指：每只手 5 根手指，每根 3 节，共 30 个指关节，每一节都能单独弯曲；根节还可以张开。
- 摆姿势的方式：
  - 点击身体部位即可选中对应关节，关节处会出现橙色球形标记；也可以在右侧“关节”列表里选择。拾取用的是骨骼线段与射线的距离，摆好姿势后点选依然准确。
  - 右侧滑块按关节的实际活动方式命名（如“前抬 / 侧抬 / 旋转”“弯曲”“低头 / 转头 / 歪头”），并限制在合理的角度范围内。
  - 直接拖动部位：上下拖动调整第一个参数，左右拖动调整第二个参数。
  - 拖动骨盆，或按住 `Ctrl`（Mac 为 `Cmd`）拖动任意部位，可以移动整个人物。
- 预设姿势：自然站立、T 字、A 字、行走、跑步、坐姿、双手上举、挥手、托腮思考。
- 手势预设：张开、放松、握拳、指向、比耶、抓握，可作用于左手、右手或双手。
- 其他：重置单个关节或全部姿势，左右镜像复制姿势。人物的颜色、线框、位置、朝向、缩放同样可调，姿势会随作品一起保存。

### 文件

| 操作 | 说明 |
| --- | --- |
| 新建 | 清空当前页签的内容（二维可撤销），需要连续点两次确认 |
| 打开 | 读取 `.json` 作品文件，同时恢复二维与三维内容 |
| 导入图片 | 把图片加入二维画布，也可以拖入或粘贴 |
| 保存 | 把二维图形、图层、导入的图片、辅助尺设置和三维物体保存为一个 `.json` 文件 |
| 导出 PNG | 二维：只导出可见图层，按内容范围裁切，2 倍分辨率；三维：导出当前视角的画面 |

---

## 快捷键汇总

| 快捷键 | 功能 |
| --- | --- |
| `V` / `H` | 选择 / 平移 |
| `M` / `O` / `Q` | 矩形选框 / 椭圆选框 / 套索 |
| `B` / `X` / `F` | 画笔 / 橡皮擦 / 油漆桶 |
| `P` / `L` / `R` / `C` / `E` / `G` | 点 / 直线 / 矩形 / 圆 / 椭圆 / 多边形 |
| `Ctrl` + `Z` | 撤销 |
| `Ctrl` + `Y` 或 `Ctrl` + `Shift` + `Z` | 重做 |
| `Ctrl` + `A` | 全选图形；在选区工具下为全选画布 |
| `Ctrl` + `C` / `Ctrl` + `X` | 拷贝 / 剪切选区内容或所选图形 |
| `Ctrl` + `J` | 把选区内容拷贝到新图层 |
| `Ctrl` + `Shift` + `I` | 反选 |
| `Ctrl` + `D` | 复制所选 |
| `Ctrl` + `S` / `Ctrl` + `O` | 保存 / 打开 |
| `Ctrl` + `V` | 粘贴（程序内拷贝的内容，或系统剪贴板中的图片） |
| `Delete` / `Backspace` | 删除所选图形；没有选中图形时清除选区内容 |
| 方向键（加 `Shift` 每次 10） | 微移所选 |
| `Esc` | 取消正在画的多边形、取消选区或取消选择 |
| `Enter` | 完成多边形 |
| 空格 + 拖动 | 平移画布 |

macOS 上用 `Cmd` 代替 `Ctrl`。

---

## 作品文件格式

保存的 `.json` 文件结构如下（当前版本 `version: 8`，旧版本文件都可以正常打开）：

```json
{
  "app": "draw-studio",
  "version": 4,
  "savedAt": "2026-09-20T08:00:00.000Z",
  "layers2d": [
    {
      "id": "L1", "name": "图层 1", "visible": true, "locked": false,
      "opacity": 1, "blend": "multiply",
      "fx": {
        "brightness": 0, "contrast": 0, "saturate": 0, "hue": 0,
        "blurType": "motion", "blur": 0, "motionDist": 40, "motionAngle": 20,
        "zoomAmt": 30, "zoomC": [400, 300]
      }
    }
  ],
  "shapes2d": [
    {
      "type": "rect", "x": 100, "y": 80, "w": 200, "h": 120,
      "stroke": "#1c2733", "lineWidth": 2, "dash": "solid",
      "fill": "solid", "fillColor": "#8fb0ff", "opacity": 1, "layerId": "L1"
    }
  ],
  "assets2d": { "img1a2b3": "data:image/png;base64,..." },
  "guides2d": {
    "type": "sym",
    "persp": { "mode": "2", "one": [400, 280], "two": [[100, 280], [700, 280]], "snap": true },
    "sym": { "mode": "radial", "n": 6, "mirror": true, "c": [400, 300] }
  },
  "objects3d": [
    {
      "kind": "box", "color": "#4f7cff", "wireframe": false,
      "position": [0, 0.75, 0], "rotation": [0, 0, 0], "scale": [1, 1, 1]
    },
    {
      "kind": "figure", "ftype": "woman", "color": "#e2bca3", "wireframe": false,
      "position": [2.4, 0, 0], "rotation": [0, 0, 0], "scale": [1, 1, 1],
      "pose": { "shoulderR": [0, 80, 80], "elbowR": [75, 0], "R_index1": [0, -10] }
    }
  ]
}
```

二维图形的类型与几何字段：

| type | 几何字段 | 说明 |
| --- | --- | --- |
| `point` | `x, y` | 点 |
| `line` | `x1, y1, x2, y2` | 直线 |
| `rect` | `x, y, w, h` | 矩形 |
| `circle` | `cx, cy, rx, ry` | 圆与椭圆 |
| `polygon` | `points: [[x, y], ...]` | 多边形 |
| `path` | `points: [[x, y, 压力], ...]` | 自由曲线；`pressure: true` 表示带笔压，`erase: true` 表示橡皮擦笔画 |
| `image` | `x, y, w, h, mask` | 油漆桶生成的填充区域，`mask` 是 PNG 遮罩的 data URL |
| `picture` | `x, y, w, h, asset` | 导入的图片，`asset` 指向 `assets2d` 中的图片数据 |

在选区内画出的图形会带有 `clip` 字段，记录当时的选区：

```json
"clip": { "rid": "r3ab", "inv": false, "ops": [
  { "op": "add", "kind": "rect", "x": 250, "y": 90, "w": 170, "h": 140 },
  { "op": "sub", "kind": "lasso", "pts": [[300, 120], [340, 180], [280, 190]] }
] }
```

`ops` 按顺序叠加（`add` 加选、`sub` 减选），`inv` 为 `true` 表示反选。清除选区内容会生成一个带 `clip` 的 `erase` 矩形；移动或拷贝选区内容会把那块区域转成一张 `picture`。

三面投影的设置保存在 `proj3d` 中，其中 `lines` 记录每条直线端点的教材坐标 `A: [x, y, z]`、`B: [x, y, z]`。

人体模型的 `ftype` 为 `man`、`woman`、`boy`、`girl`；`pose` 记录每个关节各参数的角度（单位为度，骨盆的“高度”为身高的百分比），没有记录的关节保持默认。

`shapes2d` 中的数组顺序就是绘制顺序，同一图层内越靠后的图形越在上面。图片数据单独存放在 `assets2d` 中，这样撤销记录里只保存图片编号，不会重复占用内存。

---

## 代码结构

程序是单个 HTML 文件（约 3400 行），由 HTML、CSS 和原生 JavaScript 组成。二维部分基于 Canvas 2D，三维部分基于 Three.js r128。脚本按功能分为以下几块：

| 模块 | 内容 |
| --- | --- |
| 通用 | 提示消息、文件保存（兼容 Claude 发布环境和普通浏览器下载） |
| 图形模型 | `bounds` 边界、`mapShape` / `moveShape` / `fitShape` 变换、`buildPath` 路径 |
| 绘制 | `drawShape`，以及图案填充、笔压笔画、填充区域着色、网格、选中框 |
| 图层合成 | `renderLayers`：每层先画到离屏画布，依次应用模糊和颜色调整，最后按透明度和混合模式合成；线性加深为逐像素计算 |
| 模糊 | 高斯模糊优先使用画布滤镜；动态模糊 `motionBlur` 与透视模糊 `zoomBlur` 用“逐次对半平均”实现，k 轮绘制得到 2^k 个均匀采样 |
| 命中检测 | `hitShape`、`pick`，以及框选图形的采样与判定 `finishRegion` |
| 像素选区 | `selRegion` 选区数据、`regionMask` 选区遮罩、`drawShape` 按 `clip` 裁剪、`captureRegion` / `floatSelection` 截取与移动选区内容、`drawAnts` 蚂蚁线 |
| 交互 | 指针事件、工具状态机、多选的移动与缩放、快捷键 |
| 填充 | `bucketFill`（扫描线泛洪 + 膨胀）、`shapeFill` |
| 辅助尺 | 透视方向吸附 `bestDir` / `projectOn`；对称变换 `symTransforms` 与 `affineShape`（仿射矩阵生成副本） |
| 导入图片 | `importImages`：读取、按需缩小、存入资源表并生成 `picture` 图形 |
| 图层面板 | 图层增删改、显示 / 锁定、混合模式与参数 |
| 历史记录 | 以快照方式实现撤销 / 重做 |
| 三维 | 场景初始化、轨道相机、射线拾取、物体变换与属性面板 |
| 三面投影 | `buildProjStatic` 投影面与轴、`buildProjDyn` 直线与投影、`pjAnalyze` 直线分类与度量、`drawUnfold` 展开图 |
| 笔刷 | `BRUSH_BASE` / `BRUSH_PRESETS` 参数与预设、`renderDabs` 笔触放置、`drawBrushStroke` 合成与颗粒、`BS_CATS` 画笔工作室分类 |
| 人体模型 | `FIG_SPECS` 体型比例、`buildFigure` 生成骨骼与蒙皮网格（放样 `tube`/`loft`/`ball` + 顶点权重）、`JC` 关节控制与角度范围、`applyPose` 应用姿势、`posePreset` / `handPose` 预设、`pickFigure` 关节拾取 |

新增一种二维图形时，需要在 `bounds`、`mapShape`、`buildPath`、`drawShape`、命中检测和 `sampleShape` 中各补一个分支，其余功能（选择、缩放、图层、保存）会自动支持。

---

## 已知限制

- **移动选区内容会转成位图**：在选区内拖动、拷贝或剪切时，那块区域会按当前缩放比例转成一张图片，之后不能再按矢量图形编辑；大幅放大后边缘可能略显模糊。
- **选区不计入撤销**：选区的创建和移动不在撤销记录中，但选区内的绘画、清除、移动都可以撤销。
- **填充区域的范围**：画布是无限延伸的，区域填充只计算当前屏幕可见范围；区域没有闭合时会填到屏幕边缘并给出提示。
- **填充区域的清晰度**：填充区域是按当前缩放比例生成的位图，之后大幅放大查看时边缘会略显模糊。
- **橡皮擦不随图形移动**：局部擦除记录的是“这里擦过”，被擦过的图形移走后，擦掉的缺口会留在原处。
- **合并图层会丢失效果**：向下合并时，上方图层的混合模式、调整参数和模糊不会保留。
- **模糊作用于整个图层**：模糊效果针对整个图层，不能只模糊某一个图形；需要局部模糊时，可以把那部分内容放到单独的图层上。
- **透视吸附的范围**：透视吸附只作用于画笔和直线工具，每一笔锁定一个方向；需要画曲线时可以临时关闭吸附。
- **对称副本是独立图形**：对称尺生成的副本在画完后是独立的图形，之后修改其中一个不会同步到其他副本。
- **导入图片会被缩小**：边长超过 2048 像素的图片会被缩小；导入的图片不会跟随对称尺生成副本。
- **白底上的变亮模式**：画布背景为白色，滤色、颜色减淡、变亮等模式需要叠在有颜色的内容上才能看出效果。
- **撤销范围**：三维场景暂不支持撤销；二维中隐藏或锁定图层不计入撤销记录。

---

## 后续可以扩展的方向

- 选区羽化、按颜色选择（魔棒）
- 笔刷共享平台：在线浏览、上传、下载 `.brush.json` 笔刷
- 二维图形旋转、对齐与分布
- 文字工具、三点透视
- 画布尺寸设置与导出 SVG
- 三维场景的撤销、光照与材质设置、导入 OBJ 模型
- 三面投影扩展到平面图形和立体（如三棱锥、带切口的长方体）的三视图
- 人体模型的反向动力学（拖动手脚自动带动整条手臂或腿）、面部细节与胖瘦肌肉量调节
